# Solidary Index

This repository was created by Solidary's index bootstrap flow.

It publishes a static GitHub Pages site for the new index and includes the core metadata for the linked Supabase project.

The provisioned Supabase project is bootstrapped automatically. In-app authentication and editor flows are intentionally disabled in this v1 scaffold.
